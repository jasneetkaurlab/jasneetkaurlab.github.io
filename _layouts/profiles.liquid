---
layout: page
---
<div class="post">
  <article>

    {% if page.bigprofiles %}
      {% for bigprofile in page.bigprofiles %}
        <hr>
        <div class="bigprofile text-center">
          {% if bigprofile.image %}
            {% assign bigprofile_image_path = bigprofile.image | prepend: 'assets/img/' %}
            {% if bigprofile.image_circular %}
              {% assign bigprofile_image_class = 'img-fluid z-depth-1 rounded-circle' %}
            {% else %}
              {% assign bigprofile_image_class = 'img-fluid z-depth-1 rounded' %}
            {% endif %}
            {% capture sizes %}(min-width: {{site.max_width}}) {{ site.max_width | minus: 30 | times: 0.05}}px, (min-width: 100px) 0.5vw, 0.5vw{% endcapture %}
            {% include figure.liquid loading="eager" path=bigprofile_image_path class=bigprofile_image_class sizes=sizes alt=bigprofile.image %}
          {% endif %}
          {% if bigprofile.more_info %}
            <div class="more-info mt-3">{{ bigprofile.more_info }}</div>
          {% endif %}
        </div>

        <div class="text-center mt-3">
          {% if bigprofile.content %}
            {% capture bigprofile_content %}{% include_relative {{ bigprofile.content }} %}{% endcapture %}
            {{ bigprofile_content | markdownify }}
          {% else %}
            {{ content }}
          {% endif %}
        </div>
      {% endfor %}
    {% endif %}


    {% if page.profiles %}
      {% for profile in page.profiles %}
        <hr>
        <div class="profile float-{% if profile.align == 'left' %}left{% else %}right{% endif %}">
          {% if profile.image %}
            {% assign profile_image_path = profile.image | prepend: 'assets/img/' %}
            {% if profile.image_circular %}
              {% assign profile_image_class = 'img-fluid z-depth-1 rounded-circle' %}
            {% else %}
              {% assign profile_image_class = 'img-fluid z-depth-1 rounded' %}
            {% endif %}
            {% capture sizes %}(min-width: {{site.max_width}}) {{ site.max_width | minus: 30 | times: 0.21}}px, (min-width: 200px) 1vw, 1vw{% endcapture %}
            {% include figure.liquid loading="eager" path=profile_image_path class=profile_image_class sizes=sizes alt=profile.image %}
          {% endif %}
          {% if profile.more_info %}
            <div class="more-info">{{ profile.more_info }}</div>
          {% endif %}
        </div>

        <div class="clearfix">
          {% if profile.content %}
            {% capture profile_content %}{% include_relative {{ profile.content }} %}{% endcapture %}
            {{ profile_content | markdownify }}
          {% else %}
            {{ content }}
          {% endif %}
        </div>
      {% endfor %}
    {% endif %}

    
  </article>
</div>
